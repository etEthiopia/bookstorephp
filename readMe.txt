name the database as BookStore

To go to an admin privileges please login as:
email : daginegussu@gmail.com
password : bookstore

to upload a book we recommend to use microsft edge browser, but
the other browsers like chrome and firefox will work too.

The admin can register book, delete user, edit user as specified in the screenshot

The user can rent and return a book.

This project contains a search, sort and other nice table features
for the admin to control user.

Do not delete any file
